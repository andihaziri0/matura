import { Module } from '@nestjs/common';

/**
 * Reserved for the AI tutor and explanation services (Vercel AI SDK).
 *
 * MVP scope: empty. Wire up only when phase 12+ tasks call for it (per docs/product/scope.md).
 */
@Module({})
export class AiModule {}
